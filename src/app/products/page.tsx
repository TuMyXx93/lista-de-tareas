import { Container, Title } from '@mantine/core';

export default function ProductsPage() {
    return (
        <Container size="lg" py="xl">
            <Title order={1}>
                Lista de Productos
            </Title>
        </Container>
    );
}